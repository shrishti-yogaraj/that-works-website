# Copy to .env and fill in values

# Sanity API token — Viewer role, for fetching posts at build time
# Create at: sanity.io/manage → nb3xydcz → API → Tokens
SANITY_READ_TOKEN=

# Sanity dataset (default: production)
VITE_SANITY_DATASET=production

# For the one-time migration script only — Editor role token
# SANITY_WRITE_TOKEN=
